{
  "name": "blockchain-crypto-toolkit",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "build":          "hardhat build",
    "test":           "hardhat test",
    "deploy:vending": "hardhat run scripts/deploy-vending.ts --network ganache",
    "deploy:ticket":  "hardhat run scripts/deploy-ticket.ts  --network ganache"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-mocha-ethers": "^3.0.0",
    "chai":    "^5.1.2",
    "hardhat": "^3.0.0-next.0"
  }
}
